# Dockerfile pour le frontend
FROM nginx:alpine

# Copier la page statique
COPY index.html /usr/share/nginx/html/index.html

# Copier la configuration nginx
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]